# テトリシュー (TETRISHOOT) AI 引き継ぎ資料 (HANDOFF.md)

本ドキュメントは、次回セッションや他のAIモデル（Claude 3.7 / Opus 等）が作業を継続・発展させるための引き継ぎリファレンスです。

---

## 1. プロジェクト基本情報

- **配置先**: `E:\ゲーム企画\tetrishoot`
- **技術選定**: Vite 6, TypeScript 5, HTML5 Canvas 2D, Web Audio API
- **動作方法**:
  ```bash
  cd "E:\ゲーム企画\tetrishoot"
  npm run dev
  ```
  ブラウザで `http://localhost:5173/` にアクセスしてプレイ可能。
- **ビルド＆型チェック**:
  ```bash
  npm run build
  ```

---

## 2. ディレクトリ・主要ファイル構成

```
E:\ゲーム企画\tetrishoot/
├── index.html                   # キャンバス・HUD・UIオーバーレイ
├── package.json                 # scripts: dev, build, preview
├── tsconfig.json                # TypeScriptコンパイラ設定
├── SPECIFICATION.md             # ゲーム設計・仕様書
├── HANDOFF.md                   # 本ファイル（引き継ぎ書）
└── src/
    ├── main.ts                  # エントリーポイント、HUD同期、60FPSゲームループ
    ├── config.ts                # 画面サイズ(540x720)、ブロック幅(30px)、時間(15s)等の全定数
    ├── core/
    │   ├── GameManager.ts       # フェーズ切り替え、面管理(1~10)、コリジョン統括
    │   ├── Input.ts             # キーボード(A/D/矢印/Space/M)＆マウス追従管理
    │   ├── Sound.ts             # Web Audio API シンセ（BGM2種＋全SE）
    │   └── Starfield.ts         # ギャラガ風多重スクロール宇宙背景
    ├── entities/
    │   ├── Tetromino.ts         # 7種ミノ定義、回転、銃口定義(O=0, T=3, 他=2)、耐久HP
    │   ├── Player.ts            # 自機、ブロック結合・ドッキング、被弾パーツ破壊、射撃
    │   ├── Enemy.ts             # ギャラガ風エイリアン（BEE, BUTTERFLY, BOSS）、編隊飛行
    │   ├── Bullet.ts            # ブロック同幅の極太自機弾、敵弾
    │   └── Wall.ts              # 左右永久壁、およびドッキングされた防壁ミノ管理
    └── effects/
        └── Particle.ts          # 爆発、火花、ドッキング光彩パーティクル
```

---

## 3. 実装済みの重要メカニクス

1. **テトリスタイム（15秒）**:
   - 上空からランダムなミノが降下。
   - 自機の上または左右に接触すると剛体ドッキング（`player.tryDock`）。
   - 左右の壁に接触させると防壁化（`wallManager.attachPiece`）。
   - 床に落ちたミノは消滅。
2. **シューティングタイム（15秒）**:
   - 自機のパーツから極太ビーム弾斉射（Oミノは0発、Tミノは3発広角、他は2発）。
   - 敵エイリアン（サイン波、ギャラガ特有のループ宙返りダイブ、ボス）。
   - 壁・防壁による敵機進路変更＆敵弾遮断。
   - 被弾パーツ破壊（HP=2）と全滅ゲームオーバー。
3. **ゲーム進行**:
   - 1面（30秒）×10面構成。5面と10面に大型ボス出現。

---

## 4. 今後の推奨拡張・ブラッシュアップ項目（アイデア）

次に作業するAIまたは開発者は以下のタスクを検討してください：

1. **ミノの回転操作**:
   - 現在は落下ミノが自動でランダム回転して降ってきますが、プレイヤーが落下中のミノを操作（↑キーで回転）できるようにするモードを追加するのも面白いです。
2. **ギャラガのトラクタービーム要素**:
   - ボスエイリアンがトラクタービームを照射し、自機のミノを1個吸い取って強奪するギミック。
   - ボスを倒すとそのミノを奪還できる演出。
3. **オンライン/ローカルランキング**:
   - `localStorage` を用いたハイスコア記録とベストステージ記録の保存。
4. **WebGL / Bloomポストプロセス**:
   - ネオン発光（Glow/Bloom）シェーダーを適用し、さらにレトロサイバー感を強調。
5. **クラウド公開**:
   - GitHub Pages や Vercel / Netlify へのワンコマンドデプロイ設定。
